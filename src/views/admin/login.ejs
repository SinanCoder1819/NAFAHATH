<style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    .admin-login-wrap {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px 16px;
        background: #0a0a0a;
    }
    .admin-login-card {
        width: 100%;
        max-width: 400px;
        background: #111;
        border: 1px solid #1e1e1e;
        border-radius: 18px;
        padding: 36px 32px 32px;
    }
    .al-title { font-size: 21px; font-weight: 700; color: #fff; text-align: center; margin-bottom: 6px; }
    .al-sub   { font-size: 13px; color: #6b7280; text-align: center; margin-bottom: 28px; }

    /* Alert */
    .al-alert {
        padding: 10px 14px;
        border-radius: 8px;
        font-size: 12px;
        font-weight: 500;
        text-align: center;
        margin-bottom: 18px;
        display: none;
    }
    .al-alert.show { display: block; }
    .al-alert-error   { background: rgba(127,29,29,0.35); color: #fca5a5; border: 1px solid rgba(127,29,29,0.6); }
    .al-alert-success { background: rgba(20,83,45,0.35);  color: #86efac;  border: 1px solid rgba(20,83,45,0.6); }

    /* Form */
    .al-group { margin-bottom: 18px; }
    .al-label {
        display: block;
        font-size: 11.5px;
        font-weight: 500;
        color: #9ca3af;
        margin-bottom: 7px;
    }
    .al-input {
        width: 100%;
        background: transparent;
        border: 1px solid #2a2a2a;
        border-radius: 9px;
        padding: 11px 14px;
        font-size: 13px;
        color: #fff;
        font-family: inherit;
        outline: none;
        transition: border-color 0.2s;
    }
    .al-input::placeholder { color: #374151; }
    .al-input:focus { border-color: #00BFFF; }
    .al-input-wrap { position: relative; }
    .al-eye {
        position: absolute;
        right: 12px;
        top: 50%;
        transform: translateY(-50%);
        background: none;
        border: none;
        cursor: pointer;
        color: #4b5563;
        padding: 0;
        display: flex;
        align-items: center;
        transition: color 0.15s;
    }
    .al-eye:hover { color: #9ca3af; }

    /* Submit */
    .al-submit {
        width: 100%;
        background: #00BFFF;
        color: #000;
        font-size: 13px;
        font-weight: 700;
        padding: 12px 0;
        border-radius: 9px;
        border: none;
        cursor: pointer;
        font-family: inherit;
        letter-spacing: 0.04em;
        transition: background 0.15s, opacity 0.15s;
        margin-top: 6px;
    }
    .al-submit:hover    { background: #009fd4; }
    .al-submit:active   { background: #0088b3; }
    .al-submit:disabled { opacity: 0.5; cursor: not-allowed; }

    /* Loading overlay */
    .al-overlay {
        display: none;
        position: fixed;
        inset: 0;
        z-index: 50;
        background: rgba(0,0,0,0.75);
        backdrop-filter: blur(4px);
        align-items: center;
        justify-content: center;
    }
    .al-overlay.show { display: flex; }
    .al-loading-box {
        display: flex;
        flex-direction: column;
        align-items: center;
        background: #111;
        border: 1px solid #1e1e1e;
        border-radius: 20px;
        padding: 36px 44px;
    }
    .al-spinner {
        width: 40px;
        height: 40px;
        border: 3px solid rgba(0,191,255,0.15);
        border-top-color: #00BFFF;
        border-radius: 50%;
        animation: al-spin 0.7s linear infinite;
        margin-bottom: 14px;
    }
    @keyframes al-spin { to { transform: rotate(360deg); } }
    .al-loading-title { font-size: 14px; font-weight: 600; color: #fff; }
    .al-loading-sub   { font-size: 12px; color: #6b7280; margin-top: 4px; }
</style>

<div class="admin-login-wrap">
    <div class="admin-login-card">

        <h1 class="al-title">Admin Login</h1>
        <p class="al-sub">Enter your credentials to access the dashboard.</p>

        <!-- Server-side error (e.g. from session flash) -->
        <% if (typeof error !== 'undefined' && error) { %>
        <div class="al-alert al-alert-error show"><%= error %></div>
        <% } %>

        <!-- JS response message -->
        <div id="alMsg" class="al-alert"></div>

        <form id="adminLoginForm" novalidate>

            <div class="al-group">
                <label class="al-label" for="alEmail">Email Address</label>
                <input type="email" id="alEmail" name="email" placeholder="Enter your email address"
                       class="al-input" autocomplete="email" required />
            </div>

            <div class="al-group">
                <label class="al-label" for="alPassword">Password</label>
                <div class="al-input-wrap">
                    <input type="password" id="alPassword" name="password" placeholder="Enter your password"
                           class="al-input" style="padding-right:42px" autocomplete="current-password" required />
                    <button type="button" id="alEyeBtn" class="al-eye" aria-label="Toggle password">
                        <svg id="alEyeOpen" xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M2.036 12.322a1.012 1.012 0 010-.639C3.423 7.51 7.36 4.5 12 4.5c4.638 0 8.573 3.007 9.963 7.178.07.207.07.431 0 .639C20.577 16.49 16.64 19.5 12 19.5c-4.638 0-8.573-3.007-9.963-7.178z"/>
                            <path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                        </svg>
                        <svg id="alEyeClosed" xmlns="http://www.w3.org/2000/svg" width="18" height="18" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" style="display:none">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M3.98 8.223A10.477 10.477 0 001.934 12C3.226 16.338 7.244 19.5 12 19.5c.993 0 1.953-.138 2.863-.395M6.228 6.228A10.45 10.45 0 0112 4.5c4.756 0 8.773 3.162 10.065 7.498a10.523 10.523 0 01-4.293 5.774M6.228 6.228L3 3m3.228 3.228l3.65 3.65m7.894 7.894L21 21m-3.228-3.228l-3.65-3.65m0 0a3 3 0 10-4.243-4.243m4.242 4.242L9.88 9.88"/>
                        </svg>
                    </button>
                </div>
            </div>

            <button type="submit" id="alSubmit" class="al-submit">Login</button>
        </form>
    </div>
</div>

<!-- Loading overlay -->
<div id="alOverlay" class="al-overlay">
    <div class="al-loading-box">
        <div class="al-spinner"></div>
        <p class="al-loading-title">Authenticating</p>
        <p class="al-loading-sub">Verifying your credentials...</p>
    </div>
</div>

<script>
(function () {
    // Eye toggle
    const pwInput   = document.getElementById('alPassword');
    const eyeOpen   = document.getElementById('alEyeOpen');
    const eyeClosed = document.getElementById('alEyeClosed');

    document.getElementById('alEyeBtn').addEventListener('click', () => {
        const show = pwInput.type === 'password';
        pwInput.type        = show ? 'text' : 'password';
        eyeOpen.style.display   = show ? 'none' : '';
        eyeClosed.style.display = show ? ''     : 'none';
    });

    // Message helper
    const msgDiv = document.getElementById('alMsg');
    function showMsg(text, type) {
        msgDiv.textContent = text;
        msgDiv.className   = 'al-alert show ' + (type === 'error' ? 'al-alert-error' : 'al-alert-success');
    }
    function hideMsg() { msgDiv.className = 'al-alert'; }

    // Form submit
    const form      = document.getElementById('adminLoginForm');
    const submitBtn = document.getElementById('alSubmit');
    const overlay   = document.getElementById('alOverlay');

    form.addEventListener('submit', async (e) => {
        e.preventDefault();
        hideMsg();
        overlay.classList.add('show');
        submitBtn.disabled = true;

        try {
            const res  = await fetch('/admin/login', {
                method:  'POST',
                headers: { 'Content-Type': 'application/json' },
                body:    JSON.stringify({
                    email:    document.getElementById('alEmail').value.trim(),
                    password: pwInput.value,
                }),
            });
            const data = await res.json();

            if (res.ok) {
                showMsg('Login successful! Redirecting...', 'success');
                setTimeout(() => { window.location.href = data.redirectUrl || '/admin/dashboard'; }, 800);
            } else {
                overlay.classList.remove('show');
                submitBtn.disabled = false;
                showMsg(data.message || 'Invalid credentials.', 'error');
            }
        } catch {
            overlay.classList.remove('show');
            submitBtn.disabled = false;
            showMsg('Connection error. Please try again.', 'error');
        }
    });
})();
</script>
